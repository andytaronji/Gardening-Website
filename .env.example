# Email configuration
SMTP_HOST=your-smtp-host.com
SMTP_PORT=465
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-password

# Note: For security, never commit the actual .env file to version control
# Instead, set these environment variables in your Vercel project settings
